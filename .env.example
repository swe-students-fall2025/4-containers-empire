# INSTRUCTIONS:
# 1. Copy this file to create your actual environment file:
#    cp .env.example .env
# 
# 2. Replace the dummy values below with your actual credentials
# 
# 3. NEVER commit the actual .env file to version control
#    (It's already in .gitignore)

# Database name (used by both ML client and web app)
MONGO_DBNAME=animal_classifier

# MongoDB host (use 'db' when running in Docker, 'localhost' for local testing)
MONGO_HOST=db

# MongoDB port (default is 27017)
MONGO_PORT=27017

# MongoDB authentication (To be changed in production)
MONGO_USERNAME=empireteam
MONGO_PASSWORD=supersecretpassword

# Full MongoDB connection URI (used by pymongo)
# Format: mongodb://username:password@host:port/database_name
MONGO_URI=mongodb://empireteam:supersecretpassword@db:27017/animal_classifier

# Path to the Keras model file 
MODEL_PATH=models/keras_model.h5

# Path to the labels file 
LABELS_PATH=models/labels.txt

# Input image size for the model (width and height in pixels)
IMAGE_SIZE=224

# Flask settings
FLASK_ENV=development
FLASK_DEBUG=1
SECRET_KEY=561092

# Web app port
WEB_APP_PORT=5000




# Notes:

# - The MONGO_HOST should be 'db' when running containers with docker-compose
#   (this is the service name defined in docker-compose.yml)
# 
# - Use 'localhost' only if connecting from outside Docker (e.g., during 
#   local development or testing)
# 
# - For PRODUCTION, change the default username/password to secure values, including MONGO_URI
# 
# - The database 'animal_classifier' will be created automatically when
#   first document is inserted